name: Dockyard
options:
  bundleIdPrefix: com.barnolacesc
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  minimumXcodeGenVersion: "2.40.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    SWIFT_OBJC_BRIDGING_HEADER: Resources/Dockyard-Bridging-Header.h
    HEADER_SEARCH_PATHS:
      - $(PROJECT_DIR)/ghostty/include
    OTHER_SWIFT_FLAGS:
      - "-Xcc"
      - "-Wno-incomplete-umbrella"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Manual
    CODE_SIGNING_REQUIRED: "NO"


packages:
  swift-cmark:
    url: https://github.com/swiftlang/swift-cmark
    from: "0.7.1"
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: "2.9.1"

targets:
  Dockyard:
    type: application
    platform: macOS
    info:
      path: Dockyard/Info.plist
      properties:
        CFBundleDisplayName: $(PRODUCT_NAME)
        CFBundleVersion: "0.2.2" # x-release-please-version
        CFBundleShortVersionString: "0.2.2" # x-release-please-version
        LSMinimumSystemVersion: $(MACOSX_DEPLOYMENT_TARGET)
        NSMainStoryboardFile: ""
        NSPrincipalClass: NSApplication
        LSUIElement: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        SUPublicEDKey: "grC1rloxT0gFZ2mQVIuDTZlkNFXwXLZhn9+xs4YGuXo="
        SUFeedURL: https://dockyard.barnola.net/appcast.xml
        SUEnableAutomaticChecks: false
        CFBundleURLTypes:
          - CFBundleURLName: com.barnolacesc.dockyard.open
            CFBundleURLSchemes:
              - $(DY_URL_SCHEME)
        NSAppleEventsUsageDescription: A program running within Dockyard would like to use AppleScript.
        NSAudioCaptureUsageDescription: A program running within Dockyard would like to access your system's audio.
        NSLocalNetworkUsageDescription: A program running within Dockyard would like to access the local network.
        NSMicrophoneUsageDescription: A program running within Dockyard would like to use your microphone.
        NSSpeechRecognitionUsageDescription: A program running within Dockyard would like to use speech recognition.
    sources:
      - path: Sources
        excludes:
          - Launcher
          - AgentStateHelper
      - path: Resources/Assets.xcassets
        buildPhase: resources
      - path: Localization/en.lproj/Localizable.strings
        buildPhase: resources
      - path: Localization/ca.lproj/Localizable.strings
        buildPhase: resources
      - path: Localization/es.lproj/Localizable.strings
        buildPhase: resources
      - path: Localization/sv.lproj/Localizable.strings
        buildPhase: resources
      - path: Localization/en.lproj/InfoPlist.strings
        buildPhase: resources
      - path: Localization/ca.lproj/InfoPlist.strings
        buildPhase: resources
      - path: Localization/es.lproj/InfoPlist.strings
        buildPhase: resources
      - path: Localization/sv.lproj/InfoPlist.strings
        buildPhase: resources
      - path: Localization/de.lproj/Localizable.strings
        buildPhase: resources
      - path: Localization/de.lproj/InfoPlist.strings
        buildPhase: resources
      - path: Resources/Scripts/dy
        buildPhase: resources
      - path: Resources/notification.wav
        buildPhase: resources
      - path: ghostty/zig-out/share/terminfo
        type: folder
        buildPhase: resources
      - path: ghostty/zig-out/share/ghostty
        type: folder
        buildPhase: resources
      - path: Resources/MonacoEditor
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_MODULE_NAME: Dockyard
        CODE_SIGN_ENTITLEMENTS: Resources/dy.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        LD_RUNPATH_SEARCH_PATHS:
          - "@executable_path/../Frameworks"
        LIBRARY_SEARCH_PATHS:
          - $(PROJECT_DIR)/ghostty/macos/GhosttyKit.xcframework/macos-arm64_x86_64
        OTHER_LDFLAGS:
          - -lghostty
          - -lz
          - -lc++
          - -framework Metal
          - -framework MetalKit
          - -framework CoreGraphics
          - -framework CoreText
          - -framework Foundation
          - -framework IOSurface
          - -framework QuartzCore
          - -framework AppKit
          - -framework IOKit
          - -framework Carbon
      configs:
        Debug:
        Debug:
          PRODUCT_BUNDLE_IDENTIFIER: com.barnolacesc.dockyard.debug
          PRODUCT_NAME: Dockyard Debug
          ASSETCATALOG_COMPILER_APPICON_NAME: AppIconDebug
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
          FF_URL_SCHEME: dockyard-debug
        Release:
          PRODUCT_BUNDLE_IDENTIFIER: com.barnolacesc.dockyard
          PRODUCT_NAME: Dockyard
          FF_URL_SCHEME: dockyard
          DY_URL_SCHEME: dockyard
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
    dependencies:
      - target: FFRun
      - target: DyAgentState
      - package: swift-cmark
        product: cmark-gfm
      - package: swift-cmark
        product: cmark-gfm-extensions
      - package: Sparkle
        product: Sparkle
    preBuildScripts:
      - script: |
          set -e
          "${PROJECT_DIR}/scripts/gen-appcommit.sh"
        name: Generate AppCommit
      - script: |
          set -e
          "${PROJECT_DIR}/scripts/build-editor.sh"
        name: Build Monaco Editor
        inputFiles:
          - $(PROJECT_DIR)/editor/package.json
          - $(PROJECT_DIR)/editor/bun.lock
          - $(PROJECT_DIR)/editor/src/main.js
          - $(PROJECT_DIR)/editor/index.html
          - $(PROJECT_DIR)/editor/vite.config.js
        outputFiles:
          - $(PROJECT_DIR)/Resources/MonacoEditor/index.html
    postBuildScripts:
      - script: |
          set -e
          mkdir -p "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers"
          cp -f "${BUILT_PRODUCTS_DIR}/dy-run" "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-run"
          chmod +x "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-run"
          codesign --force --sign "${EXPANDED_CODE_SIGN_IDENTITY}" --timestamp --options=runtime --generate-entitlement-der "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-run"
        name: Bundle dy-run
        outputFiles:
          - $(TARGET_BUILD_DIR)/$(CONTENTS_FOLDER_PATH)/Helpers/dy-run
      - script: |
          set -e
          mkdir -p "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers"
          cp -f "${BUILT_PRODUCTS_DIR}/dy-agent-state" "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-agent-state"
          chmod +x "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-agent-state"
          codesign --force --sign "${EXPANDED_CODE_SIGN_IDENTITY}" --timestamp --options=runtime --generate-entitlement-der "${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers/dy-agent-state"
        name: Bundle dy-agent-state
        outputFiles:
          - $(TARGET_BUILD_DIR)/$(CONTENTS_FOLDER_PATH)/Helpers/dy-agent-state

  FFRun:
    type: tool
    platform: macOS
    preBuildScripts:
      - script: |
          set -e
          "${PROJECT_DIR}/scripts/gen-appcommit.sh"
        name: Generate AppCommit
    sources:
      - path: Sources/Launcher
      - path: Sources/Models/AppConstants.swift
      - path: Sources/Models/AppCommit.swift
      - path: Sources/Models/FilePersistence.swift
      - path: Sources/Models/RunState.swift
    settings:
      base:
        PRODUCT_NAME: dy-run
        OTHER_LDFLAGS:
          - -lproc
      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG

  DyAgentState:
    type: tool
    platform: macOS
    preBuildScripts:
      - script: |
          set -e
          "${PROJECT_DIR}/scripts/gen-appcommit.sh"
        name: Generate AppCommit
    sources:
      - path: Sources/AgentStateHelper
      - path: Sources/Models/AgentState.swift
      - path: Sources/Models/AppConstants.swift
      - path: Sources/Models/AppCommit.swift
      - path: Sources/Models/FilePersistence.swift
      - path: Sources/Models/RunState.swift
    settings:
      base:
        PRODUCT_NAME: dy-agent-state

  DockyardTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: Dockyard
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.barnolacesc.dockyard.tests
        GENERATE_INFOPLIST_FILE: true
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Dockyard Debug.app/Contents/MacOS/Dockyard Debug"
